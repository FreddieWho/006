# 空间置换证据

- `moran_999_permutations.tsv`：71捕获/2769项，2752可估计，17不可估计，含实际置换次数与kernel来源。
- `RUN_RECEIPT.json`：输入、运行代码、输出hash及COMPUTATION_COMPLETE状态。
- 原表：`results/v7/spatial_discovery/topology_increment.tsv`仅用于核对观测Moran，不作为真实增量。
- 缺失：患者层级总体推断、独立GT/完整基线topology比较仍未运行；不能以高精度置换填补它们。
