# 历史归档

本目录保存被 v7 取代但仍需溯源的文档。

- `plans/`：v5、v6.1、v6.2 科学方案和路线；
- `operations/`：v6 执行合同、审计和 realignment 文档；
- `data_inventory_legacy/`：旧数据收集表；
- `reports/`：不再作为当前入口的报告和 pathway 探索输出；
- `environments/`：历史环境清单，不作为 v7 依赖锁。

归档不等于证据有效。是否可被 v7 使用以 `docs/v7/V6_ASSET_DISPOSITION.md` 和 `results/v7/inheritance/asset_manifest.tsv` 为准。
