# v6.2.1 Agent-orchestrated Roadmap

# PD-1-anchored immune failure mechanism and X-class repair framework

中文名：

# v6.2.1 施工路线图：以 PD-1 为锚的免疫治疗失败机制与 X-class 修复框架

---

## v6.2.1 版本说明

本版本基于 v6.2 scientific plan 与 v6.2 roadmap，在 Phase5–6 已完成后的真实项目状态上重排 Phase7 及之后的依赖关系。

版本规则：

1. **Phase0–Phase6 的正文逐字继承 v6.2 原文，不回写、不重命名、不重新开放 Phase6 算法选型。**
2. Phase6 当前唯一主入口固定为已冻结的 response-blind module backbone；后续只做语义、测量、残差化与资格审计，不重新启动 WGCNA/NMF/LDA 等方法比赛。
3. Phase7 起使用 v6.2.1 新顺序，覆盖原 v6.2 roadmap 中 Phase7–18 的施工安排；scientific plan 的核心问题、claim boundary 与 Result Layer A/B/C 定义不变。
4. v6.2.1 的主要修正是：
   - 将 Phase6 封口补强与 barrier identifiability 分开；
   - 将 anti-PD1 anchor/context adjudication 前移到 SRB 复杂表示之前；
   - 将 SRB Stage-A 从默认必经步骤改为 necessity gate；
   - 将 perturbation coverage 与 X-class ontology 前移到 counterfactual repair 之前；
   - 将 spatial 与 external validation 并行前置到 evidence card v0；
   - 将 ZSL 改为满足前置证据条件后的可选上行层。
5. 本文件仍是 roadmap，不是具体 phase prompt。每个探索阶段预置至少两条可并行技术路线，但 Controller 必须依据同一 frozen input、evaluation harness 和预注册 gate 决定是否采纳。

当前执行基线：

- Phase5 与 Phase6 已完成；
- 已冻结 8 个 response-blind candidate expression topics；
- 这些 topics 是机制坐标候选，不等同于 anti-PD1 failure modules，也不等同于 dominant barriers；
- Phase7 的第一职责是修复“测量对象不清、composition/lineage/functional state 混合、预指定程序覆盖不足”的问题，而不是立即做 response 命名。

---

## 0. Roadmap 定位

本文件是 v6.2 的施工路线图。它不重复 scientific plan 的全部科学论证，也不包含具体 phase prompt。它定义：

1. 阶段顺序；
2. 各 lane 的并行关系；
3. 每阶段核心输入、任务、输出；
4. Go / No-Go / downgrade gate；
5. 哪些地方可用 AutoResearch-style 迭代升级；
6. 每个关键技术节点允许探索的技术路线范围；
7. 哪些结果可进入 Result Layer A/B/C；
8. 何时启动独立 agent / session；
9. 何时需要 integration rehearsal 和 hostile reviewer audit。

Revision note, 2026-06-24:
added explicit Spatial Data Standardization Gate to make existing Phase 2 / Phase 11 spatial requirements executable without changing scientific scope.

Revision note, 2026-06-24 (2) — 生物学补强挂钩（依据 `results/v6_2/PLAN_ROADMAP_BIOLOGICAL_REVIEW_20260624.md`，用户批准）：
已并入 B1 + B2；B3–B6 落档为补强审阅挂钩，进行到对应 phase 时**必须唤起**该挂钩做补强审阅。

| 挂钩 | 内容 | 唤起 phase | 是否已并入 |
|---|---|---|---|
| B1 | HCC 病因分层：**可插拔、低权重、HCC-context 限定**；默认不进 pan-cancer 主线、不主导 IO 重点；覆盖不足则关闭 | Phase 2（审覆盖）、Phase 4（打标签）、Phase 9（HCC residual sensitivity 协变量） | 是 |
| B2 | 模块本体补强：TLS/B、neutrophil-NET、tumor-intrinsic WNT exclusion、progenitor/terminal exhaustion（仍 response-blind） | Phase 4（refinement）、Phase 6（注释/稳定性雷达）、Phase 11（TLS/APC-T niche） | 是 |
| B3 | 当代 X-class + 临床失败组合（LEAP-002、COSMIC-312）作 held-out 负样本；anti-TIGIT 作正向用例 | Phase 12、Phase 13 | 否（挂钩） |
| B4 | primary / adaptive / acquired resistance 分型（数据允许时） | Phase 9、Phase 10 | 否（挂钩） |
| B5 | TCR 克隆替换 vs 再激活 作 responder 签名（有 paired TCR 时前移为推荐项） | Phase 9 | 否（挂钩） |
| B6 | 显式声明 perturbation 先验对 myeloid/stromal/TLS/中性粒的覆盖偏倚（缺支持≠证据为负） | Phase 12、Phase 14 | 否（挂钩） |

本 roadmap 的基本原则是：

**先让数据、模块和评测系统可靠，再训练 SRB；先证明 dominant barrier 可辨识，再做 repair；先有 spatial/perturbation/external 的证据接口，再让 ZSL 排序；先形成 evidence card，再决定论文 claim。**

---

## 1. 总体阶段结构

v6.2 采用三阶段结构。

### Stage 1：Rescue / Prebuild

目标：确认输入可信、资产可用、MVP 污染切断、关键数据覆盖足够，建立所有后续迭代依赖的评测和冻结机制。

输出目标：

* source-of-truth 锁定；
* data go/no-go；
* frozen input family；
* evaluation harness；
* iteration log；
* immune-state feature contract；
* anchor / spatial / perturbation / external bulk 覆盖审计；
* Stage 2 是否可启动的 decision。

### Stage 2：Core Build

目标：完成 v6.2 的核心机制模型和证据链第一轮闭环。

输出目标：

* response-blind modules；
* barrier identifiability gate；
* SRB Stage-A latent；
* anti-PD1 anchor verdict；
* shared/context-modulated modules；
* SRB transport / CRTN repair direction；
* SNGM spatial niche support；
* perturbation coverage gate；
* X-class descriptor ontology；
* ZSL alignment first-pass；
* 至少一个无关键冲突的 candidate evidence card v0。

Stage 2 的硬底线：

**如果无法形成至少一个无关键冲突的 candidate evidence card v0，不进入 Stage 3。**

### Stage 3：Manuscript Package

目标：将可防守的 candidate 组织成论文主线、主图、补图、证据卡、复现包和 claim boundary。

输出目标：

* Result Layer A/B/C 中至少一个层级完整成立；
* 主图故事；
* candidate evidence cards；
* external / spatial / perturbation validation package；
* hostile reviewer audit；
* manuscript-ready claim boundary；
* minimal reproducibility package。

---

## 2. 工作 lane 设计

v6.2 不按单线程 pipeline 执行，而按 lane 并行推进。

### Lane A：Control / Governance Lane

角色：Controller agent 常驻。

负责：

* source-of-truth；
* frozen input；
* branch agent 启动/关闭；
* handoff 审查；
* gate verdict；
* integration rehearsal；
* hostile reviewer audit；
* 关键科学决策提交给用户确认。

### Lane B：Asset / Data Lane

角色：data asset agent。

负责：

* cohort registry；
* metadata；
* response label environment；
* data go/no-go；
* anchor/spatial/perturbation/external bulk/TCR 覆盖审计；
* patch proposal。

### Lane C：Immune-state / Module Lane

角色：immune-state agent + module agent。

负责：

* immune-state construction；
* feature family audit；
* response-blind module discovery；
* module annotation；
* barrier identifiability gate。

### Lane D：SRB / ML Core Lane

角色：SRB agent。

负责：

* module-factorized latent；
* SRB Stage-A pretraining；
* context head；
* anchor head；
* transport head；
* alignment head；
* ablation；
* calibration。

### Lane E：Spatial Lane

角色：spatial asset / SNGM agent。

负责：

* spatial asset audit；
* niche construction；
* statistical niche baseline；
* SNGM if data supports；
* spatial support/conflict verdict。

### Lane F：Perturbation / X-class / ZSL Lane

角色：perturbation-ZSL agent。

负责：

* X-class descriptor ontology；
* perturbation coverage gate；
* target / pathway / drug family mapping；
* Combination ZSL；
* Mechanism ZSL；
* ZSL leakage / abstention audit。

### Lane G：Evidence / Manuscript Lane

角色：integration / manuscript agent。

负责：

* evidence cards；
* conflict log；
* claim tier；
* result layer assembly；
* figure source tables；
* manuscript skeleton；
* hostile reviewer audit.

---

## 3. 公共基础设施

### Phase 0.1：Project Control Brief

目的：

建立所有 agent / session 共同遵守的轻量项目宪法。

核心内容：

* v6.2 project identity；
* 当前 scientific plan 为最高科学依据；
* MVP-derived 结果不继承，只能作为 raw data locator / failure audit；
* HCC 是 pre-specified index cancer，不是典型癌种；
* ZSL 是 formal extrapolation layer，不是机制证明；
* SRB 是主模型，SNGM 独立空间验证；
* graph attention 不等于 causal edge；
* perturbation prior 是 soft condition + coverage gate；
* no clinical recommendation；
* branch agent 不可修改 frozen input；
* spike 默认不能自动升主线。

交付：

* `AGENT.md`
* `source_of_truth_and_no_mvp_inheritance.md`
* `claim_boundary.md`

Gate：

* AGENT.md 必须短而硬；
* 不写具体 prompt；
* 不写复杂局部 AGENT.md；
* 能被所有 branch agent 读懂。

Agent 编排：

* Controller agent 执行；
* 用户确认后冻结。

---

### Phase 0.2：Evaluation Harness

目的：

建立统一评测系统，防止每个 agent 用自己的指标宣布胜利。没有这个，AutoResearch-style 迭代会变成随机调参祭祀。

核心功能：

* patient-level split；
* leave-one-cohort；
* leave-one-cancer；
* leave-center-out；
* response-label environment split；
* permutation null；
* negative control；
* bootstrap CI；
* calibration；
* abstention metric；
* external direction consistency；
* spatial permutation null；
* perturbation direction consistency；
* model ablation registry。

交付：

* `eval_harness_spec.md`
* `eval_registry.csv`
* `iteration_log_template.md`
* `negative_control_catalog.md`

Gate：

* 所有后续模型必须向 eval harness 写入结果；
* 任何模型如果没有 baseline 和 negative control，不允许进入 evidence card；
* 所有迭代必须记录 seed、input version、parameter hash、metric、adopt/reject decision。

[AUTO-RESEARCH 入口]

从这一阶段开始，所有可迭代技术点必须使用同一循环：

1. 提出可证伪假设；
2. 生成一个小改动；
3. 在固定 frozen input + eval harness 上运行；
4. 写入 iteration log；
5. 与 v0 baseline 比较；
6. 只采纳能提升预注册指标且不增加关键冲突的改动。

技术探索范围：

主路线：

* 自实现统一评测器；
* 指标和 split 固定；
* 每次迭代显式登记。

降级路线：

* 如果完整 eval harness 过重，先实现 core split + bootstrap + permutation + calibration。

禁止：

* 临时换 split；
* 调完才定义指标；
* 只报告最佳 seed；
* 不记录失败实验。

---

## 4. Stage 1：Rescue / Prebuild

### Phase 1：Source-of-truth and Inheritance Lock

目的：

防止旧方案、MVP、v6.1 偏移结果和新 v6.2 方案互相污染。

任务：

1. 汇总当前可用 plan、review、roadmap、ZSL 报告和 v6.1 完成结果；
2. 标记哪些文件是 source-of-truth，哪些只是背景；
3. 明确 v6.1 资产需要重新审计后才能进入 v6.2；
4. 明确 MVP-derived feature、archetype、heuristic MOA、virtual shift、demo counterfactual 禁止进入 v6.2；
5. 明确可接受 raw locator / failure audit 的范围。

交付：

* `source_of_truth_matrix.md`
* `inheritance_policy.md`
* `legacy_asset_quarantine_list.csv`

Gate：

* 所有后续 phase 只能读取 approved frozen asset；
* 旧文件中与当前 v6.2 plan 冲突者自动降级为 historical context；
* MVP 任何 derived result 不得作为主输入。

Agent：

* Controller agent；
* 如文件很多，可启动 asset-audit branch agent；
* 合并后由 Controller 冻结。

技术探索范围：

无模型探索。这里要保守、清晰、可审计。

---

### Phase 2：Data Go / No-Go Audit

目的：

在模型训练前确认关键数据是否足以支撑 SRB、CRTN、SNGM、ZSL 和外部验证。这个阶段不做漂亮图，只回答“能不能做”。

审计对象：

1. clean anti-PD1 / ICI anchor；
2. expanded / support anchor；
3. paired pre/post ICI data；
4. observed PD1+X / combination data；
5. HCC index cancer data；
6. spatial / tissue-niche data；
7. perturbation / target prior data；
8. external bulk / clinical anchor；
9. TCR support；
10. response-label environment metadata。

任务：

* 样本量审计；
* response label 质量审计；
* treatment context 审计；
* patient/sample duplication 审计；
* timepoint 审计；
* platform/cohort/batch 审计；
* X-class coverage 审计；
* spatial metadata 审计；
* perturbation coverage 审计；
* （B1 挂钩）HCC etiology 元数据覆盖审计（HBV/HCV/alcohol/NASH-MAFLD/其他）；仅决定可插拔低权重 sensitivity 层能否启用，覆盖不足则关闭并记缺口，不影响主线；
* 数据缺口与可补数据列表。

交付：

* `data_go_no_go_report.md`
* `cohort_registry_v6_2_candidate.csv`
* `response_label_environment_table.csv`
* `data_coverage_matrix.csv`
* `data_gap_register.csv`
* `stage1_data_gate.yaml`

Gate：

* 若 clean anchor 不足，R1 降级为 R1-lite 或 R2；
* 若 paired displacement 不足，CRTN 最高档禁用；
* 若 spatial 数据不足，SNGM 降级为 statistical niche adjudication；
* 若 perturbation coverage 低，ZSL 和 transport 中 perturbation loss 降权；
* 若 response label environment 不清，所有 supervised claim 降级。

Agent：

* Data asset agent 独立执行；
* Controller 审核并冻结 `data_coverage_matrix`；
* 高风险数据补充需要用户确认。

[AUTO-RESEARCH 标注]

这里不是算法迭代，而是可进行 **data acquisition search iteration**：

* 检索新增 spatial HCC ICI 数据；
* 检索 PD1+X paired / pre-post 数据；
* 检索 perturb-seq / LINCS target coverage；
* 每次新增数据必须通过同一 go/no-go schema。

技术探索范围：

主路线：

* 只补能直接修复 evidence gap 的数据。

并行探索：

* spatial 补强；
* PD1+X observed combination 补强；
* external bulk HCC ICI 补强；
* perturbation coverage 补强。

禁止：

* 为了增加样本数引入 metadata 不明的高混杂数据；
* 把高混杂联合治疗并入 clean anchor；
* 因数据稀缺而修改科学问题。

---

### Phase 2.5：Spatial Data Standardization Gate

目的：

把 downloaded spatial assets 转成可审计、可复现、可降级的 v6.2 spatial input family。该阶段只定义空间数据能否进入 Phase 11，不改变 SRB / SNGM / Evidence Card 的科学边界。

任务：

1. 建立 raw archive / extracted file 索引；
2. 按 Visium、Xenium、MERFISH、CosMx、GeoMx / region-level 等平台分类；
3. 建立 spatial sample metadata 最小表；
4. 对齐 expression matrix、coordinates、image、scale factor 和 sample ID；
5. 标记 patient、tissue、cancer、treatment、pre/post、response / failure label 的可用性；
6. 执行样本级 QC；
7. 记录 platform-specific fallback；
8. 冻结可进入 Phase 11 的 standardized spatial input。

交付：

* `spatial_raw_asset_manifest.csv`
* `spatial_sample_metadata_minimal.csv`
* `spatial_standardization_manifest.csv`
* `spatial_qc_report.csv`
* `spatial_standardization_failure_log.md`

Gate：

* 缺少 expression + coordinates 的样本不能进入 Phase 11；
* metadata 不足的样本只能作为 localization support；
* 缺少 response / treatment label 的样本不能支持 response / failure claim；
* image / coordinate 不齐或空间质量不足的样本不能训练 SNGM；
* GeoMx / region-level 数据进入 region-localization fallback，不作为 full spatial graph；
* 任何 raw download 目录不得绕过本 gate 直接进入 Phase 11。

Agent：

* Spatial asset agent 执行；
* Controller 审核并冻结 `spatial_standardization_manifest.csv`；
* SNGM agent 只能读取通过本 gate 的 standardized spatial input。

技术探索范围：

主路线：

* 保守解包、最小 schema、样本级 QC、显式降级。

可靠降级：

* 只生成 raw asset registry 和 localization support list；
* 对无法标准化的数据只进入 failure log。

禁止：

* 为了训练 SNGM 强行补齐缺失 metadata；
* 把 image / coordinate 不齐的样本作为 graph evidence；
* 把没有 response / treatment label 的空间数据写成 failure evidence；
* 让空间标准化结果改变 v6.2 的科学问题。

---

### Phase 3：Frozen Input Family

目的：

把 Stage 2 所需输入冻结，防止多 agent 并行时各自用不同 metadata / feature matrix。

任务：

1. 生成或审计 cohort registry；
2. 生成 sample/patient metadata master；
3. 建立 response-label environment；
4. 建立 canonical patient split；
5. 建立 feature family eligibility；
6. 建立 raw-to-feature provenance；
7. 标记 frozen input version。

交付：

* `cohort_registry.frozen_v0.csv`
* `patient_metadata_master.frozen_v0.csv`
* `sample_metadata_master.frozen_v0.csv`
* `patient_split.frozen_v0.csv`
* `feature_family_eligibility.frozen_v0.csv`
* `frozen_input_manifest.yaml`

Gate：

* branch agent 只能读取 frozen input；
* 修改必须提交 `patch_proposal`；
* Controller 才能发布 frozen_v0.1 / v1.0；
* 所有输出必须声明 input version。

Agent：

* Controller + data asset agent；
* 后续所有 branch agent 启动前必须读取 frozen manifest。

技术探索范围：

无高级模型。只允许 schema 和 QC 改进。别在这里发明宇宙，宇宙已经够乱了。

---

---

### Phase 3.5：Single-cell Processing QC Gate

目的：

把 Phase3 冻结的 scRNA / scTCR / processed object / rescue expression 资产转成可审计、可追溯、可进入整合的标准分析对象。该阶段不做生物学结论，不做模块发现，不做 supervised modeling，不做完整 atlas claim。它只回答一个问题：

**这些单细胞对象是否足够干净、可追踪、可整合，能否安全进入 Phase4A / Phase4B。**

本阶段是 Phase3 Frozen Input Family 与 Phase4 immune-state construction 之间的工程质量门。Phase3 只冻结 metadata、role、split、manifest 和使用边界；Phase3.5 冻结 expression object、QC、HVG、layer decision、barcode/sample/patient 对齐和 integration readiness。

输入：

* `frozen_input_manifest.yaml`
* `cohort_registry.frozen_v0.csv`
* `sample_metadata_master.frozen_v0.csv`
* `patient_metadata_master.frozen_v0.csv`
* `patient_split.frozen_v0.csv`
* `response_label_environment.frozen_v0.csv`
* `dataset_role_and_feature_eligibility.frozen_v0.csv`
* `label_inventory_and_semantic_review.csv`
* Phase3 formal intake / spatial gate / expression rescue ledger
* downloaded or local scRNA objects：raw count、processed Seurat RDS、h5ad、MTX、H5、metadata、TCR files、normalized expression objects

任务：

1. 建立 raw / processed object processing manifest。
   对每个 cohort 记录原始文件、对象类型、路径、hash、assay/layer、metadata 来源、是否可读取、是否与 `frozen_v0` 对齐。

2. 建立 matrix layer decision。
   对每个 cohort 明确后续使用哪一层：raw counts、filtered raw counts、normalized expression、processed Seurat assay、h5ad layer、pseudobulk rescue、support-only matrix。
   禁止 raw count 和 normalized matrix 混用而不记录。

3. 统一 gene identifier。
   完成 Ensembl ID / gene symbol / duplicated gene symbol / mitochondrial-ribosomal-stress genes 的标准化记录。
   输出 gene overlap、gene loss、duplicated gene handling 和 immune-core gene coverage。

4. 对齐 sample / patient / cell barcode。
   确保每个 cell 可以追溯到 `cohort_id`、`sample_id`、`patient_id`、`sample_key`、`patient_key`。
   对多 library / multi-timepoint / sorted population / pre-post 数据建立 library-to-sample mapping。
   不允许 cell barcode 离开 sample context 后直接合并。

5. 执行 sample-level 和 cell-level QC。
   至少记录每个样本的 cell 数、gene 数、UMI 数、mitochondrial fraction、ribosomal fraction、hemoglobin / platelet / dissociation / stress signal、low-quality cell fraction、empty / ambient RNA 风险、doublet 风险。
   允许使用已有 QC-pass object，但必须记录其 QC 来源和不可重复风险。

6. 建立 filtering decision log。
   对每个 cohort / sample 记录过滤前后细胞数、过滤阈值、过滤原因、是否采用原作者 QC、是否追加本项目 QC。
   低细胞数、低基因覆盖、严重 batch 或 metadata 不完整的样本进入 downgraded / support-only，不得悄悄删除。

7. 建立 TCR / GEX join QC。
   对有 TCR 的 cohort 记录 GEX-TCR join key、join 率、matched cell 数、unmatched TCR 数、unmatched GEX 数、clonotype 字段可用性。
   TCR join 不清楚的 cohort 不进入 TCR feature 主线，只能作为 support。

8. 建立 cell annotation source inventory。
   记录每个 cohort 的原始 celltype 字段、annotation 层级、是否有 immune / stromal / tumor coarse label、是否有 myeloid / T / NK / CAF / endothelial 细分、annotation 来源、可信度和是否需要 Phase4A 重新映射。

9. 冻结 HVG strategy。
   HVG 选择必须 response-blind。
   至少输出三类 HVG：

   * pan-cancer global HVG；
   * major-cell-state-stratified HVG；
   * caution / exclusion HVG list。
     caution list 应标记 mitochondrial、ribosomal、hemoglobin、stress、cell-cycle、dissociation、platform-dominated、single-cohort-dominated genes。
     HVG 不得按 responder / non-responder 选择。

10. 执行 integration readiness audit。
    对每个 cohort 和每个 major cell compartment 判断是否可进入 full integration、coarse integration、reference mapping only、pseudobulk only、support-only 或 excluded。
    审计内容包括 gene overlap、cell count、sample count、batch covariates、platform、sample composition、annotation availability、raw/normalized layer clarity。

11. 建立 batch covariate registry。
    冻结后续 integration 可用的 covariates：cohort、study、platform、chemistry、sample、patient、cancer type、tissue、timepoint、treatment context、library batch、sorting strategy。
    response label 只能作为 evaluation / stratification / leakage audit 变量，不能作为 integration covariate 直接参与校正。

12. 生成 Phase4A / Phase4B handoff。
    明确哪些对象进入 integration，哪些只进 pseudobulk，哪些只做 support，哪些被排除。
    所有 downstream 分析必须读取 Phase3.5 标准对象或 handoff manifest，不能直接读取 raw download 目录。

交付：

* `sc_processing_manifest.frozen_v0.csv`
* `matrix_layer_decision_table.frozen_v0.csv`
* `gene_id_harmonization_report.md`
* `gene_overlap_and_immune_core_coverage.csv`
* `cell_qc_summary_by_sample.csv`
* `cell_filtering_decision_log.csv`
* `sample_cell_barcode_index.csv`
* `gex_tcr_join_qc_report.csv`
* `cell_annotation_source_inventory.csv`
* `hvg_global_pan_cancer.tsv`
* `hvg_by_major_cell_state.tsv`
* `hvg_caution_or_exclusion_list.tsv`
* `batch_covariate_registry.csv`
* `integration_readiness_report.md`
* `analysis_object_registry.frozen_v0.csv`
* `phase3_5_to_phase4_handoff.yaml`

Gate：

* 样本 / 病人 key 无法与 `frozen_v0` 对齐的对象不能进入 Phase4A / Phase4B。
* 表达层类型不清楚的对象不能进入 full integration。
* raw count / normalized layer 混不清的对象只能 support-only。
* QC 后细胞数过低的样本只能 support-only 或 excluded。
* gene overlap 或 immune-core gene coverage 过低的 cohort 不能进入 full pan-cancer integration。
* HVG 被单一 cohort / platform / stress genes 主导时，必须重做 HVG 或降级为 compartment-specific HVG。
* TCR join 不清楚或 join 率过低时，不生成主线 TCR features。
* cell annotation 来源不清的 cohort 不能进入 fine-grained annotation claim，只能进入 coarse-level 或 Phase4A 重新映射。
* response label、outcome、split、survival 字段不得进入 expression feature construction。
* 所有输出必须声明 `input_version=frozen_v0`。

Agent：

* Data processing agent 主执行。
* Immune-state agent 参与 QC 标准和 HVG 设计。
* Controller 审核 `phase3_5_to_phase4_handoff.yaml`。
* 如 QC / annotation / layer 决策争议较大，可启动短期 adversarial data QC reviewer。

[AUTO-RESEARCH 标注]

这里不是生物学模型迭代，而是 processing / QC strategy iteration。
允许小范围比较：

* QC threshold；
* HVG strategy；
* doublet / ambient filtering strategy；
* raw-count vs normalized-object reuse strategy；
* gene ID harmonization strategy。

评价指标：

* retained cell fraction；
* retained sample fraction；
* immune-core gene coverage；
* cross-cohort gene overlap；
* HVG cohort dominance；
* QC covariate balance；
* downstream integration readiness；
* annotation recoverability；
* TCR join quality。

技术探索范围：

主路线：

* 保守 QC；
* response-blind HVG；
* 明确 layer decision；
* 样本级可追溯；
* cohort-level 降级优先于强行合并。

可靠降级：

* full integration → coarse integration；
* coarse integration → reference mapping only；
* reference mapping only → pseudobulk only；
* pseudobulk only → support-only；
* support-only → registry-only。

禁止：

* 直接从 raw download 目录绕过 Phase3.5 进入 Phase4。
* 为了增加样本量强行合并低质量或 layer 不明对象。
* 用 response / outcome 指导 QC、HVG 或 annotation。
* 在没有 barcode / sample / patient 对齐的对象上生成 patient-level feature。
* 把作者已处理对象直接当作可信输入而不记录 assay、layer、QC 来源。
* 为了追求整合漂亮而过度校正 cancer / treatment / biological state。

---

### Phase 4A：Single-cell Integration and Cell-state Harmonization

目的：

在 Phase3.5 通过 QC 和 integration-readiness 的对象上，构建 response-blind 的统一 cell-state annotation 和 integration reference。该阶段回答：

**不同队列的细胞类型和细胞状态能否被放到同一个可比较的生物学坐标系里。**

Phase4A 不生成最终 patient-level modeling matrix，不做 response association，不做 module discovery。它只负责 cell-state integration、annotation harmonization、细胞状态可靠性和后续 Phase4B 的 cell-state ontology。

输入：

* `phase3_5_to_phase4_handoff.yaml`
* `analysis_object_registry.frozen_v0.csv`
* `cell_qc_summary_by_sample.csv`
* `cell_annotation_source_inventory.csv`
* `hvg_global_pan_cancer.tsv`
* `hvg_by_major_cell_state.tsv`
* `hvg_caution_or_exclusion_list.tsv`
* `batch_covariate_registry.csv`
* Phase3 frozen metadata / split / response environment
* integration-ready scRNA objects

任务：

1. 建立 cell-state ontology v6.2。
   至少包含 coarse → mid → selected fine 三层。
   coarse 层覆盖 T/NK、B/plasma、myeloid、DC/APC、CAF/stromal、endothelial、tumor-like、cycling、unknown / low-confidence。
   mid / fine 层覆盖 CD8 cytotoxic、CD8 dysfunctional、CD8 progenitor-like、CD8 terminal exhaustion-like、Treg、NK、inflammatory myeloid、suppressive myeloid、macrophage、C1QC-like macrophage、TREM2-like macrophage、S100A9-like monocyte、DC/APC、TLS/B cell、CAF、endothelial、vascular / angiogenic-like、tumor-intrinsic WNT-exclusion-like 等。
   B2 挂钩必须纳入 TLS/B、neutrophil/TAN/NET、tumor-intrinsic WNT exclusion、CD8 progenitor / terminal exhaustion 的注释与稳定性雷达，但仍保持 response-blind。

2. 执行 marker-based harmonization。
   用 curated marker registry 和 canonical immune / stromal / tumor markers 进行粗到中层映射。
   marker 只能用于 annotation，不得按 response 方向手动挑选。

3. 执行 reference mapping。
   使用合适的 public reference / project reference / existing annotations 进行 label transfer。
   对 annotation conflict 生成 conflict matrix，而不是直接覆盖。

4. 执行 integration model comparison。
   可比较 scVI、scANVI、Harmony、Seurat integration、Scanorama 或其他轻量路线。
   integration 目标是 cell-state comparability，不是消除所有 cohort difference。
   cancer type、treatment context、timepoint 中可能包含真实生物差异，不能无脑校正掉。

5. 评估 integration 质量。
   同时评估 batch mixing、celltype preservation、marker enrichment、known lineage separation、rare-state preservation、cancer/cohort overcorrection risk。
   不允许只用 UMAP 视觉判断。

6. 评估 annotation 可靠性。
   对每个 cell state 输出 reliability score，包括 marker support、reference agreement、cross-cohort reproducibility、sample coverage、cell count、conflict rate。
   低可靠性细分状态必须降级到 coarse ontology。

7. HCC index-context refinement。
   对 HCC 相关 myeloid、macrophage、CAF、endothelial、vascular、tumor-like 状态进行单独可靠性审计。
   如果 myeloid / stromal / endothelial 细分不稳定，后续 HCC context modulation 不允许写细粒度 barrier claim，只能写 broader myeloid-stromal / vascular barrier。

8. pan-cancer annotation consistency audit。
   检查主要 cell-state label 是否跨 cancer / cohort 可复现。
   如果某个 cell state 只存在于单一 cohort 或由单一平台驱动，标记为 cohort-specific / sensitivity-only。

9. 生成 cell-level annotation master。
   每个 cell 记录 `cell_key`、`cohort_id`、`sample_key`、`patient_key`、原始 annotation、harmonized annotation、ontology level、reliability、integration batch、低质量标记、是否进入 Phase4B aggregation。

10. 生成 Phase4B aggregation eligibility。
    明确每个 sample / cell state 是否可用于 fraction、pseudobulk、signature/pathway/TF、TCR feature、HCC refinement、support-only analysis。

交付：

* `cell_state_ontology_v6_2.yaml`
* `cell_state_annotation_master.csv`
* `cell_state_annotation_conflict_matrix.csv`
* `integration_method_comparison_report.md`
* `integration_qc_metrics.csv`
* `cell_state_reliability_report.md`
* `hcc_context_cell_state_refinement_report.md`
* `myeloid_stromal_endothelial_reliability_radar.csv`
* `b2_module_ontology_annotation_radar.csv`
* `phase4a_to_phase4b_aggregation_eligibility.csv`
* `phase4a_decision_manifest.yaml`

Gate：

* 若 integration 破坏 lineage / major cell type separation，则不得采用该 integration 作为主 annotation 坐标。
* 若 batch mixing 好但 marker preservation 差，视为 overcorrection 风险，不得进入主线。
* 若 marker-based 和 reference-mapping 严重冲突，必须降级为 coarse ontology 或进入 adversarial annotation review。
* 若 rare cell state 只在单一 cohort / platform 出现，不得作为 pan-cancer shared state。
* 若 HCC myeloid / CAF / endothelial 细分不可靠，不得写细粒度 HCC barrier claim。
* 若 cell annotation reliability 不足，Phase4B 只能聚合到上一级 ontology。
* response label 不得参与 annotation、integration、cell-state naming。

Agent：

* Immune-state agent 主执行。
* Data processing agent 提供 object / QC 支持。
* Controller 审核是否满足 Phase4B aggregation。
* 对 myeloid / stromal / exhaustion / TLS 注释争议，可启动 adversarial annotation reviewer。

[AUTO-RESEARCH 标注]

可迭代点：

* integration algorithm；
* integration covariates；
* HVG subset；
* ontology resolution；
* marker panel；
* reference atlas；
* myeloid / CAF / endothelial / exhaustion subtyping granularity。

评价指标：

* marker enrichment；
* reference agreement；
* cross-cohort label consistency；
* batch mixing；
* celltype preservation；
* rare-state preservation；
* overcorrection risk；
* spatial mappability；
* downstream Phase4B feature completeness。

技术路线探索范围：

主路线：

* marker-based harmonization + reference mapping 双轨；
* coarse-to-mid ontology 优先；
* fine-grained subtype 必须可靠性过门；
* response-blind annotation。

可靠降级：

* fine subtype → mid-level state；
* mid-level state → coarse lineage；
* integrated annotation → reference mapping only；
* full atlas → per-cancer / per-compartment atlas；
* cell-level fine state → patient-level broad fraction。

并行探索：

* scVI / scANVI latent as auxiliary；
* scFM embedding as optional alignment support；
* HCC-specific sub-atlas；
* myeloid / stromal compartment-specific integration。

禁止：

* 用 response label 命名 cell state。
* 手动挑 marker 迎合预期机制。
* 把 UMAP 好看当成 integration 成功。
* 为了 pan-cancer 一致性抹掉 cancer / organ context biology。
* 在低可靠性细胞亚型上写强机制 claim。
* 未经过 Phase3.5 的对象不得进入 Phase4A。

---

### Phase 4B：Patient-timepoint Immune-state Feature Construction

目的：

把 Phase4A 可靠的 cell-state annotation 和 Phase3.5 标准分析对象聚合为 patient-timepoint 级 immune-state feature matrix，为 Phase5 strong baseline、Phase6 response-blind module discovery、Phase8 SRB Stage-A 和后续 anchor / repair / ZSL 提供统一输入。

该阶段回答：

**每个病人、每个时间点的免疫-肿瘤状态，如何被稳定、可解释、可建模地表示。**

Phase4B 不做最终 responder 发现，不做 dominant barrier 判定，不做 X-class repair。它只构建 feature family，并记录 missingness、confounding、eligibility 和 response-label environment 绑定。

输入：

* `phase4a_decision_manifest.yaml`
* `phase4a_to_phase4b_aggregation_eligibility.csv`
* `cell_state_annotation_master.csv`
* `cell_state_reliability_report.md`
* `analysis_object_registry.frozen_v0.csv`
* `matrix_layer_decision_table.frozen_v0.csv`
* `gex_tcr_join_qc_report.csv`
* Phase3 frozen metadata / split / response environment
* curated gene sets：immune signatures、pathway sets、TF regulons、LR pairs、immune-core gene universe
* optional TCR assets passing Phase3.5 QC

任务：

1. 建立 analysis universe registry。
   不按过细数据角色拆碎主分析，而是建立统一主分析池并附加条件标签。
   至少包含：

   * `primary_pan_cancer_scRNA_universe`
   * `anchor_calibration_subset`
   * `endpoint_sensitivity_subset`
   * `treatment_cleanliness_sensitivity_subset`
   * `hcc_index_context_subset`
   * `support_validation_pool`
   * `excluded_or_registry_only_pool`
     数据角色作为 sample/cohort 标签与敏感性条件保留，不作为默认拆分主分析的理由。

2. 生成 cell-state fraction features。
   按 patient-timepoint / sample 计算 coarse、mid、reliable fine cell states 的 fraction。
   对低细胞数或低可靠性 cell state 进行 shrinkage / censoring / reliability tag。
   输出 raw fraction、normalized fraction、composition-aware transform 和 QC covariates。

3. 生成 cell-state-specific pseudobulk。
   只对通过细胞数、样本数、gene coverage 和 annotation reliability 的 cell state 生成 pseudobulk。
   pseudobulk 必须保留 sample / patient / timepoint / cell-state provenance。
   低细胞数 cell state 只能 broad-bin pseudobulk 或 excluded。

4. 生成 pathway / TF / signature activity。
   在合适层级上计算 pathway、TF regulon、immune signature、exhaustion / cytotoxic / IFN / APC / antigen presentation / myeloid suppression / Treg / CAF / endothelial / TLS / WNT exclusion 等 scores。
   评分方法可以比较 AUCell、GSVA、ssGSEA、decoupler / PROGENy / DoRothEA 或同类方法。
   所有 signature 必须 response-blind 定义。

5. 生成 optional TCR features。
   仅对通过 GEX-TCR join QC 的 cohort 生成 TCR features。
   可包括 clone expansion、clonotype diversity、expanded clone fraction、T cell state × clone coupling、paired pre/post clone persistence / replacement。
   若 paired TCR 不足，则 TCR features 标记为 optional / sensitivity-only。

6. 生成 tumor / stromal / immune QC covariates。
   包括 tumor purity proxy、immune fraction proxy、stromal fraction proxy、cell count、gene count、UMI、mitochondrial fraction、batch、platform、cohort、cancer type、tissue source、timepoint、treatment context 等。
   这些 covariates 后续用于 confounding audit，不应被混作生物机制主特征。

7. 绑定 response-label environment。
   将 response endpoint、RECIST / pathologic response、treatment cleanliness、monotherapy / combo、pre/post、timepoint schema、cohort、cancer type 作为 environment / stratification / sensitivity variables。
   response label 不进入 response-blind module discovery 的 feature construction。

8. 生成 feature dictionary。
   每个 feature 必须有 family、source、level、cell state、gene set、method、input layer、eligible universe、missingness、confounding risk、allowed downstream use。
   允许 downstream use 至少包括：baseline、module discovery、SRB input、anchor calibration、sensitivity-only、support-only、excluded。

9. 生成 feature missingness and confounding audit。
   对每个 feature family 和 analysis universe 计算 missingness、cohort dominance、cancer dominance、platform dependence、cell-count dependence、sample-quality dependence。
   高 missingness 或高 cohort dominance 的 feature family 不进入主线，只能 sensitivity-only。

10. 生成 patient-timepoint immune-state feature matrix。
    输出统一矩阵，行单位为 patient-timepoint 或 sample-timepoint，列为通过 eligibility 的 feature。
    同时输出主矩阵、feature-family 矩阵和 sensitivity 矩阵。
    所有矩阵必须可回溯到 Phase3 frozen input、Phase3.5 processing 和 Phase4A annotation。

11. 生成 Phase5 handoff。
    明确哪些 feature family 和 universe 可进入 strong baseline / confounding audit，哪些只能 sensitivity，哪些被 blocked。
    后续 Phase5 不得直接绕过 Phase4B 读取 cell-level object 构造新 feature。

交付：

* `analysis_universe_registry_v6_2.csv`
* `cell_state_fraction_matrix.csv`
* `cell_state_specific_pseudobulk_registry.csv`
* `cell_state_specific_pseudobulk_matrix.parquet`
* `pathway_tf_signature_activity_matrix.csv`
* `optional_tcr_feature_matrix.csv`
* `sample_patient_timepoint_qc_covariates.csv`
* `immune_state_feature_matrix.frozen_v0.csv`
* `immune_state_feature_matrix_by_family/`
* `feature_dictionary_v6_2.csv`
* `feature_missingness_report.csv`
* `feature_confounding_tags.csv`
* `response_environment_binding_table.csv`
* `phase4b_to_phase5_handoff.yaml`
* `PHASE4_IMMUNE_STATE_FEATURE_CONSTRUCTION_REPORT.md`

Gate：

* 主矩阵必须使用 patient-level split，不允许同一 patient 跨 train / val / test。
* feature column 中不得包含 response、outcome、survival、split、treatment response 等泄漏字段。
* 高 missingness feature family 不进入 primary universe。
* 强 cohort / cancer / platform dominance 的 feature 必须标记 confounding risk。
* 低可靠性 cell state 不生成 fine-level patient feature，只能生成 coarse feature。
* 低细胞数 pseudobulk 不进入主线。
* TCR join 不过门的 cohort 不生成 TCR 主特征。
* pathologic response 与 RECIST 不得合并为同质监督标签，只能通过 endpoint sensitivity 处理。
* support-only / bulk / spatial / combo 数据不得替代 clean scRNA anchor。
* 所有 supervised downstream model 只能通过 Phase5 使用 patient-level split 和 Phase4B handoff。

Agent：

* Immune-state agent 主执行。
* Data processing agent 维护 matrix/provenance。
* Controller 审核 feature dictionary、universe registry 和 handoff。
* Phase5 baseline agent 只允许读取 `phase4b_to_phase5_handoff.yaml` 中允许的矩阵与 universe。

[AUTO-RESEARCH 标注]

可迭代点：

* aggregation level；
* pseudobulk minimum cell threshold；
* signature/pathway/TF scoring method；
* feature family inclusion rule；
* reliability-weighted feature construction；
* compositional transform；
* TCR feature definition；
* missingness / confounding threshold。

评价指标：

* feature completeness；
* missingness；
* cohort dominance；
* cancer dominance；
* cell-count dependence；
* marker/pathway biological sanity；
* cross-cohort reproducibility；
* spatial mappability；
* downstream baseline utility；
* negative-control leakage risk。

技术路线探索范围：

主路线：

* patient-timepoint aggregation；
* response-blind feature construction；
* feature dictionary + confounding tags；
* unified primary analysis pool with conditional sensitivity labels；
* strict handoff to Phase5。

可靠降级：

* fine cell-state features → mid/coarse features；
* cell-state-specific pseudobulk → lineage-level pseudobulk；
* pathway/TF/signature family → curated signature only；
* TCR main features → TCR sensitivity-only；
* primary universe → support/sensitivity universe。

并行探索：

* reliability-weighted aggregation；
* compartment-specific feature matrices；
* HCC index-context feature layer；
* anchor calibration subset feature layer；
* endpoint sensitivity feature layer。

禁止：

* 用 response label 构造 feature。
* 为了提高下游 AUC 临时增加 response-derived feature。
* 把 support-only 数据混入 clean anchor supervised universe。
* 在 feature construction 阶段做模块发现或机制 claim。
* 让 Phase5 / Phase6 绕过 Phase4B 直接从 cell-level object 重新造 feature。
* 把数据角色拆成互相孤立的小分析，破坏 pan-cancer 主线。

---

## 5. Stage 2：Core Build

### Phase 5：Strong Baseline and Confounding Audit

目的：

建立所有复杂模型的压力 benchmark。它不是为了最高分，而是判断信号是否已经被简单 feature、cell fraction 或 cohort/cancer confounding 解释。

任务：

1. cell fraction baseline；
2. curated signature baseline；
3. pathway / TF baseline；
4. pseudobulk module baseline；
5. elastic net / logistic；
6. limited XGBoost / random forest；
7. scVI/patient latent baseline；
8. negative controls；
9. label leakage audit；
10. cancer/cohort/batch/cell fraction confounding audit。

交付：

* `baseline_results.csv`
* `baseline_model_manifest.yaml`
* `confounding_audit_report.md`
* `negative_control_report.md`
* `feature_family_downstream_eligibility.csv`

Gate：

* 如果 baseline 显示信号主要来自 cohort/cancer/batch，相关 feature 降级；
* 如果 cell fraction baseline 已解释主要结果，后续机制模型必须证明增量不是 cell fraction surrogate；
* 如果 response leakage，停止监督分析并修复；
* 如果简单 baseline 已达到极强但机制不清，SRB 仍可做解释模型，但不能只靠预测性能宣传。

Agent：

* Baseline agent；
* 独立 hostile statistics reviewer；
* Controller 决定哪些 feature family 进入 module / SRB。

[AUTO-RESEARCH 标注]

可迭代点：

* regularization；
* feature family；
* split strategy；
* calibration；
* missingness handling；
* class imbalance handling。

评价指标：

* LOCO performance；
* leave-center-out performance；
* calibration；
* negative control collapse；
* cell-fraction sensitivity；
* response-environment robustness。

技术路线探索范围：

主路线：

* interpretable baseline + limited strong ML。

可靠降级：

* signature/pathway/fraction-only baseline；
* cohort-specific descriptive statistics。

并行探索：

* scVI latent baseline；
* simple MLP patient classifier as stress test；
* domain-adversarial baseline if cohort leakage severe。

禁止：

* baseline 变成模型动物园；
* 只看 AUC；
* 在 patient split 外混用同患者样本；
* 用 baseline 高分直接做机制 claim。

---

### Phase 6：Response-blind Module Discovery

目的：

构建 SRB 的 module ontology 和 latent 分块单位。module 是后续 barrier gate、SRB、SNGM、ZSL、evidence card 的共同语义单位。

任务：

1. response-blind module discovery；
2. module membership；
3. module scoring；
4. carrier cell-state annotation；
5. pathway / TF / LR annotation；
6. module stability；
7. spatial mappability；
8. perturbation mappability；
9. module version freeze。

交付：

* `module_membership.v0.csv`
* `module_score_matrix.v0.csv`
* `module_attribute_table.v0.csv`
* `module_stability_report.md`
* `module_mappability_report.md`
* `module_freeze_manifest.yaml`

Gate：

* 模块必须 response-blind 产生；
* 模块必须可在 patient level 评分；
* 不稳定高分辨率模块合并；
* 不能用 response-derived module 进入 SRB；
* 模块数量过多会导致 SRB 不稳，必须压缩到可解释范围。

Agent：

* Module agent；
* Parallel method comparison agent 可单独执行 WGCNA/NMF/sparse AE 对比；
* Controller 决定 module freeze。

[AUTO-RESEARCH 标注]

可迭代点：

* WGCNA-like resolution；
* NMF rank；
* graph community resolution；
* sparse autoencoder bottleneck；
* gene universe；
* cell-state-specific vs pan-cell module。

评价指标：

* bootstrap membership stability；
* cross-cohort reproducibility；
* pathway coherence；
* carrier cell-state clarity；
* spatial mappability；
* perturbation mappability；
* downstream SRB reconstruction utility。

技术路线探索范围：

主路线：

* WGCNA/NMF/graph community consensus；
* sparse dictionary refinement as upgrade。

可靠降级：

* broader immune programs；
* curated immune programs；
* pathway-level modules。

并行探索：

* sparse autoencoder modules；
* topic modeling；
* cell-state-specific module discovery。

禁止：

* response-supervised module discovery；
* 为得到漂亮机制强行拆小模块；
* 不稳定模块进入 dominant barrier。

---

### v6.2.1 下游执行覆盖说明

从本节开始，以下安排覆盖原 v6.2 roadmap 的 Phase7–18。Phase0–6 的产物、输入契约和冻结规则继续有效。

依赖关系重排如下：

| v6.2 原阶段 | v6.2.1 新位置 | 调整理由 |
|---|---|---|
| Phase7 Barrier Identifiability | Phase7A + Phase7B | 先解决语义/测量有效性，再判断 barrier 是否可分，避免把 composition topic 直接当机制 |
| Phase8 SRB Stage-A | Phase9 SRB Necessity Gate | 当前只有少量冻结 topics，SRB 必须先证明相对直接 module model 的增量价值 |
| Phase9 Context/Anchor | Phase8 Anchor and Context Adjudication | response direction 必须先于复杂 latent 和 transport 冻结 |
| Phase12 Perturbation/X-class | Phase10 | X-class descriptor 和 coverage 是 X-conditioned transport 的前置输入，不能后补 |
| Phase10 CRTN/Transport | Phase11 | 先完成 anchor 与 X-class action space，再做 generic 和 X-conditioned repair |
| Phase11 Spatial + Phase15 External | Phase12A + Phase12B 并行 | evidence card v0 前必须已有正交空间/外部裁判，而不是卡片完成后再补证据 |
| Phase14 Evidence Card | Phase13 v0 + Phase15 final | 先形成不含 ZSL 的 candidate card v0 过 Stage2 gate，再决定是否上行 ZSL |
| Phase13 ZSL | Phase14 Conditional ZSL | ZSL 是外推层，不再是默认关键路径 |

v6.2.1 下游 lane 覆盖：

- Lane C 拆为 **Module Measurement Lane** 与 **Barrier Identifiability Lane**；
- Lane D 的 SRB 由“默认主干”改为“与 direct module backbone 竞争后决定是否启用”；
- Lane F 的 perturbation coverage / X-class ontology 前移，先定义 action space，再允许 transport；
- Lane E Spatial 与 Lane G External Validation 在 Phase12 并行；
- Evidence Card v0 是 Stage2 的硬 gate；ZSL 只能在 v0 卡片和标签条件满足时启动。

---

### Phase 7A：Frozen Module Semantic and Measurement Validity

目的：

对 Phase6 已冻结的 response-blind modules 做封口补强，明确它们究竟测量了什么、在哪个层级评分、受哪些 composition / lineage / tissue / QC 信号驱动，并建立从 cell-state expression unit 到 patient-timepoint 的唯一评分契约。

本阶段不重新发现模块，不分析 response 方向，不把任何 module 命名为 resistance / failure barrier。

Phase6 封口原则：

1. Phase6 的算法选型永久关闭；
2. 8 个冻结结果统一称为 `frozen candidate expression topics`，名称保持 provisional；
3. 允许在 Phase7A 后合并、降级或改为 context/sensitivity topic，但不允许为了获得预期机制重新训练模块；
4. data-driven modules 与 prespecified sentinel programs 双轨保留，避免“未被 LDA 抽取”被错误解释为“不存在”。

输入：

* `phase6_to_phase7_module_entry_manifest.yaml`
* Phase6 frozen module membership / score / top-gene / bootstrap / cross-method support outputs
* Phase4A cell-state annotation 与 reliability outputs
* Phase4B patient-timepoint feature matrix、cell fractions、QC covariates、pseudobulk provenance
* Phase5 confounding / negative-control outputs
* B2 prespecified programs：TLS/B、neutrophil/TAN/NET、tumor-intrinsic WNT exclusion、CD8 progenitor / terminal exhaustion

任务：

1. 校验 Phase6 frozen assets 与唯一入口，生成不可变的 `phase6_closeout_manifest_v6_2_1.yaml`。
2. 建立三级 module activity contract：
   - cell-state expression-unit activity；
   - sample/patient-timepoint observed activity；
   - composition/lineage/QC-adjusted within-state functional activity。
3. 将 observed score 分解为至少以下来源：
   - cell-state composition；
   - lineage identity；
   - tissue / cancer / cohort / platform；
   - QC / stress / dissociation；
   - within-cell-state functional residual。
4. 对 FM02/FM03、FM05/FM08 等疑似重叠 topic 做 redundancy audit，但不预设必须合并。
5. 对 FM01 immediate-response/stress、FM04 stromal/vascular/stress、FM07 liver/myeloid/metabolic identity 做 source-of-signal audit。
6. 重做 module biological annotation：top genes、pathway/TF、carrier states、cross-cohort coverage、known-signature overlap；所有命名保留置信等级。
7. 建立 B2 sentinel program lane：对 TLS/B、neutrophil/NET、WNT exclusion、CD8 progenitor/terminal exhaustion 单独评分和稳定性审计；sentinel 不能自动升级为 frozen module，只能获得 `mainline_candidate / sensitivity_only / unsupported` 状态。
8. 生成 patient-timepoint module scoring contract；禁止 downstream agent 绕过该 contract 从 cell-level object 临时重算分数。
9. 形成 Phase7B 唯一允许读取的 residualized module matrix 与 module eligibility table。

并行技术方案：

#### 方案 A：分层残差化与混合效应分解（保守主线）

* 在 cell-state / sample / patient 层建立 variance-component 或 mixed-effect model；
* 显式回归 composition、cohort、cancer、platform、QC covariates；
* 输出 observed score 与 within-state residual score；
* 优点：透明、样本需求低、容易审计；
* 风险：线性假设可能不足，复杂共线结构处理有限。

#### 方案 B：多层贝叶斯或层级 shrinkage measurement model（增强路线）

* 联合估计 cell-state、patient、cohort 与 cancer 层随机效应；
* 对低细胞数或低覆盖状态进行 partial pooling；
* 输出 posterior module activity 与不确定性；
* 优点：能处理不均衡和小队列，适合 patient-timepoint 评分；
* 风险：模型设定和收敛更复杂，必须与方案 A 对照。

#### 可选方案 C：稳健矩阵分解 / orthogonalization stress test

* 用 robust PCA、partial residual projection 或 orthogonal score 对 composition / tissue identity 做压力测试；
* 只作为敏感性验证，不得替代 A/B 的可解释主输出。

评价指标：

* patient-timepoint score completeness；
* variance explained by composition / lineage / cohort / QC；
* within-state residual reproducibility；
* bootstrap score stability；
* carrier-state consistency；
* provisional annotation coherence；
* sentinel program cross-cohort coverage；
* A/B 路线输出方向与排序的一致性。

交付：

* `phase6_closeout_manifest_v6_2_1.yaml`
* `module_activity_by_cell_state.csv`
* `module_activity_by_patient_timepoint_observed.csv`
* `module_activity_by_patient_timepoint_residualized.csv`
* `module_variance_decomposition.csv`
* `module_redundancy_and_overlap_audit.csv`
* `module_semantic_annotation_v6_2_1.csv`
* `module_name_confidence_and_claim_boundary.csv`
* `prespecified_sentinel_program_scores.csv`
* `prespecified_sentinel_program_eligibility.csv`
* `phase7a_to_phase7b_handoff.yaml`
* `PHASE7A_MODULE_MEASUREMENT_VALIDITY_REPORT.md`

Gate：

* 每个 module 必须有明确 scoring level 和 provenance；
* composition/lineage/QC 无法拆分的 module 不进入 fine-grained barrier claim；
* 主要由单一 tissue/cancer/cohort 驱动的 topic 标记为 context-only 或 sensitivity-only；
* immediate-early/stress 主导的 topic 默认 sensitivity-only，除非 within-state residual 稳定且有正交支持；
* sentinel program 未达到稳定性不得补进 frozen module set；
* Phase6 算法方法不得因本阶段结果不理想而重新开放。

Agent：

* Module measurement agent 主执行；
* hostile annotation / measurement reviewer 独立审查；
* Controller 冻结 Phase7B 输入。

禁止：

* 使用 response label 帮助 residualization、命名或合并；
* 把细胞比例变化直接写成 cell-intrinsic mechanism；
* 重新跑 module discovery 来迎合 B2 程序；
* 让 provisional topic name 进入主 claim。

---

### Phase 7B：Barrier Identifiability Gate

目的：

在 composition/lineage/QC-adjusted module space 中判断哪些 candidate programs 可以被稳定区分，哪些只能合并为 coarse barrier，哪些完全不能进入 dominant barrier 或 repair。

该阶段只裁定“能否区分”，不裁定“与 response 的方向是什么”。

输入：

* `phase7a_to_phase7b_handoff.yaml`
* residualized patient-timepoint module matrix
* observed module matrix 与 composition components
* bootstrap resamples
* sentinel program eligibility
* module carrier-state / mappability attributes

任务：

1. 计算 module score correlation、partial correlation、condition number 和 variance inflation；
2. 执行 patient/cohort bootstrap，计算 assignment flip rate 与 rank stability；
3. 执行 multi-resolution / scoring-method sensitivity，但不得重做 module discovery；
4. 检验 FM02/FM03、FM05/FM08 等相邻 topics 是否在 within-state residual 中仍可分；
5. 检验 broad myeloid、Treg、stromal/vascular、APC/IFN、cytotoxic/exhaustion 等 barrier families 的可辨识性；
6. 评估 carrier-state consistency、spatial mappability、perturbation mappability；
7. 生成 deterministic merge map 和 unstable barrier log；
8. 冻结 `eligible_barrier_set_v0`，供 Phase8 使用。

并行技术方案：

#### 方案 A：确定性可辨识性闸门（主线）

* bootstrap stability + collinearity + cross-resolution consistency；
* 层次聚类或 graph community 只用于建议 merge；
* 通过预注册阈值产生 `eligible / merge / context-only / blocked`；
* 优点：清晰、可复现、适合 hostile review。

#### 方案 B：概率潜在类 / Bayesian factor comparison（增强路线）

* 比较相邻 modules 是独立因子还是同一潜变量的不同载荷；
* 输出 factor separation posterior、assignment uncertainty；
* 优点：能显式表达“不确定是否可分”；
* 风险：小样本下先验敏感，不能单独决定主线。

#### 可选方案 C：graph-based barrier family consolidation

* 结合 score correlation、carrier state 与 pathway attributes 构建 module similarity graph；
* 只用于辅助 coarse barrier merge，不赋予因果含义。

评价指标：

* bootstrap support；
* flip rate；
* condition number；
* factor separation；
* A/B merge recommendation agreement；
* downstream association stability under merge choices；
* spatial / perturbation mappability retention。

交付：

* `barrier_identifiability_table_v6_2_1.csv`
* `eligible_barrier_set_v0.csv`
* `coarse_barrier_merge_map_v6_2_1.csv`
* `barrier_assignment_uncertainty.csv`
* `unstable_or_blocked_barrier_log.md`
* `phase7b_to_phase8_handoff.yaml`
* `PHASE7B_BARRIER_IDENTIFIABILITY_REPORT.md`

Gate：

* 未通过 gate 的 module 不进入 Phase8 response association 或 Phase11 repair；
* 共线 programs 必须使用 coarse merge 或并列 sensitivity，不得硬拆 dominant barrier；
* 至少一个 barrier family 可辨识，才允许继续 Result Layer A 主线；
* 若所有 modules 均主要是 lineage/composition，项目回退到 resource / state-atlas story，不进入 repair。

Agent：

* Barrier gate agent 主执行；
* hostile statistics reviewer 独立复核；
* Controller 冻结 eligible barrier set。

Integration rehearsal 7：

Phase7A 与 Phase7B 结束后必须进行第一次 v6.2.1 下游合练，确认：

* Phase6 frozen input 未被重训污染；
* patient-timepoint scoring contract 唯一；
* data-driven module 与 sentinel program 边界清楚；
* eligible barrier set 可供 response analysis；
* merge / blocked decision 有机器可读记录。

---

### Phase 8：Anti-PD1 Anchor and Context Adjudication

目的：

在训练复杂 SRB 表示之前，用最透明的统计框架判断 eligible barriers 是否与 anti-PD1 response/failure 方向存在跨队列可复现关系，并建立 HCC index-context residual 与 R1 / R1-lite / R2 route verdict。

本阶段把原 v6.2 Phase9 前移。它是 response supervision 首次进入主线的位置。

输入：

* `phase7b_to_phase8_handoff.yaml`
* frozen response-label environment
* clean / expanded / support anchor registries
* patient-level split
* endpoint mapping 与 treatment-cleanliness flags
* Phase5 negative-control / confounding results

Phase8 前置数据修复：

1. 明确 GSE301741 是 expression-bearing calibration cohort，还是 metadata-only candidate；未完成 object loading / scoring 时不得计入 expression anchor 样本数。
2. 核对 GSE206325 source-proven endpoint；若仍为 unknown/mixed，只能作为 treatment-clean support，不进入最高可信 supervised anchor。
3. GSE286827 仅允许 D-only 进入 clean/conditional lane，D+T 单独进入 support/sensitivity。
4. RECIST、mRECIST、pathologic response 不合并为同质主 endpoint；必须 endpoint-stratified。

任务：

1. 冻结 `anchor_environment_v6_2_1.csv`，标记 expression availability、endpoint、treatment cleanliness 和 evidence weight；
2. 对每个 eligible barrier 做 within-cohort association；
3. 对 endpoint-specific effects 做 random-effects / partial-pooling 合并；
4. 执行 leave-one-cohort、leave-one-cancer、leave-one-endpoint；
5. 调整 composition residual、QC、cancer、cohort、timepoint；
6. 检验 shared effect 与 context interaction；
7. 对 HCC 计算 index-context residual，但不允许 HCC 大样本量支配 pan-cancer shared direction；
8. B1 HCC etiology 仅在覆盖达到预注册阈值后作为低权重 sensitivity covariate；
9. 形成 responder-compatible direction 与 anti-PD1 failure direction；
10. 给出 R1 / R1-lite / R2 / Layer-A-only route verdict。

并行技术方案：

#### 方案 A：within-cohort effect + random-effects meta-analysis（保守主线）

* 每个 cohort / endpoint 独立估计 effect；
* 使用随机效应合并，报告异质性与 prediction interval；
* HCC context 用 interaction / residual meta-regression；
* 优点：不会让大 cohort 或混合 endpoint 掩盖结构问题。

#### 方案 B：层级贝叶斯 partial pooling（增强路线）

* barrier effect 分解为 shared global、cancer residual、cohort residual、endpoint residual；
* 对小队列适度 shrinkage，并输出 posterior direction probability；
* 优点：适合稀疏、不均衡 anchor；
* 风险：先验与层级设定必须做敏感性分析。

#### 可选方案 C：invariance / domain-generalization stress test

* 使用 invariant prediction、IRM-style penalty 或 domain-adversarial probe 检验 shared direction；
* 只作压力测试，不替代 A/B 的可解释 effect estimates。

评价指标：

* within-cohort direction consistency；
* random-effects heterogeneity；
* leave-one-cohort/cancer stability；
* endpoint sensitivity；
* metadata-only / cohort-only negative control collapse；
* cell-fraction adjustment sensitivity；
* HCC residual reproducibility；
* calibration 与 abstention coverage。

交付：

* `anchor_environment_v6_2_1.csv`
* `barrier_response_association_by_cohort.csv`
* `barrier_response_meta_analysis.csv`
* `shared_antipd1_barrier_candidates.csv`
* `context_modulated_barrier_candidates.csv`
* `hcc_index_context_residuals_v6_2_1.csv`
* `responder_compatible_direction_v0.csv`
* `anchor_reliability_report_v6_2_1.md`
* `route_verdict_v6_2_1.yaml`
* `phase8_to_phase9_handoff.yaml`

Gate：

* 不允许用一次 pooled association 产生主结论；
* clean anchor 强且方向跨队列稳定：R1；
* clean anchor partial、endpoint heterogeneity 可控：R1-lite；
* pan-cancer anchor 不稳但 HCC residual 稳定：R2；
* anchor 与 context 均弱：Result Layer A descriptive/resource only；
* 至少一个 eligible barrier 在预注册敏感性分析中方向稳定，才允许进入 Phase9/11。

Agent：

* Anchor/context agent 主执行；
* endpoint adjudication reviewer 与 hostile statistics reviewer 并行；
* Controller 锁定 route verdict。

禁止：

* 让 HCC 细胞数代替 HCC response patient 数；
* 把 RECIST、mRECIST、pathologic response 无条件合并；
* support/combination cohort 反推 clean anti-PD1 anchor；
* 在 anchor 未冻结前训练 supervised SRB heads。

---

### Phase 9：SRB Necessity and Representation Gate

目的：

检验 Structured Repair Backbone Stage-A 是否相对直接 residualized module representation 提供真实增量。SRB 不再是默认必经仪式；如果它只是对少量 frozen topics 再套一层 latent，则关闭或简化。

输入：

* `phase8_to_phase9_handoff.yaml`
* eligible barrier matrix
* responder-compatible direction
* context / anchor effect estimates
* Phase5 baseline registry
* evaluation harness

任务：

1. 定义 direct module backbone 作为强基线；
2. 比较静态 module matrix、PCA/NMF、简单 hierarchical module model 与 SRB Stage-A；
3. 对所有路线执行完全相同的 split、LOCO、negative control 和 leakage audit；
4. 检验 representation 是否主要编码 cohort/cancer/batch；
5. 检验 module interpretability 是否保留；
6. 检验 representation 对 Phase8 anchor/context 的 out-of-cohort 稳定性；
7. 以 downstream transport readiness，而非训练 loss，决定是否启用 SRB；
8. 冻结 `representation_route`：direct module / linear latent / SRB Stage-A。

并行技术方案：

#### 方案 A：Direct Module Backbone（强制保留的保守路线）

* residualized module scores + mixed/hierarchical heads；
* 可加 PCA/NMF 作为低维辅助，但所有输出必须可回投到原 module；
* 优点：参数少、适合有限患者数、机制解释直接。

#### 方案 B：Module-factorized SRB Stage-A（增强路线）

* shared encoder 或 module-specific blocks；
* reconstruction + graph/locality regularization；
* environment-aware leakage control；
* 优点：可能提高非线性表示、context decomposition 和 transport 接口；
* 风险：当前 module 数较少时容易成为冗余套娃。

#### 可选方案 C：Bayesian hierarchical latent / factor model

* 作为 A 与 B 之间的中复杂度路线；
* 适合估计 latent uncertainty 和 partial pooling。

评价指标：

* reconstruction / held-out likelihood；
* cohort/cancer/batch leakage；
* module interpretability preservation；
* anchor/context LOCO stability；
* negative-control collapse；
* representation stability across seeds；
* downstream generic repair baseline utility；
* 参数量与有效患者数之比。

交付：

* `representation_baseline_comparison.csv`
* `direct_module_backbone_manifest.yaml`
* `srb_stageA_model_manifest.yaml`（若运行）
* `representation_leakage_audit.csv`
* `representation_interpretability_report.md`
* `representation_route_verdict.yaml`
* `phase9_to_phase10_handoff.yaml`

Gate：

SRB 只有在至少一项产生稳定、可复现增益时才晋级：

* 降低 cohort/cancer leakage；
* 提高 anchor/context 跨队列稳定性；
* 提高 reconstruction 且不损失 module 可解释性；
* 明显改善 held-out displacement / repair readiness。

若 SRB 不优于 direct backbone：

* 正式关闭 SRB Stage-A 主线；
* 后续 Phase11 在 direct module space 中执行；
* scientific plan 中 SRB 降级为 optional implementation，而不是强行保留。

Agent：

* representation comparison agent 统一执行；
* SRB branch 与 direct-model branch 并行；
* Controller 依据同一 eval registry 做 adopt/reject。

禁止：

* 只因神经网络更新或 loss 更低就启用 SRB；
* 使用 response AUC 单指标决定 representation；
* 无 leakage audit 进入 transport；
* 为保住原 roadmap 而保留无增量模型。

---

### Phase 10：Perturbation Coverage and X-class Descriptor Ontology

目的：

在任何 X-conditioned repair 之前，先定义可计算、可审计的 X-class action space，量化每个 barrier 的 perturbation coverage，并区分“无覆盖”“方向冲突”和“负证据”。

本阶段前移原 v6.2 Phase12，是 Phase11B 与 Phase14 的硬依赖。

输入：

* eligible barrier set 与 module attributes
* representation route verdict
* perturbation resources / LINCS / perturb-seq / target databases
* observed PD1+X / combination registry
* B3 当代 X-class 与临床成功/失败组合挂钩
* B6 coverage bias 挂钩

任务：

1. 构建 X-class ontology，至少覆盖：
   - myeloid reprogramming；
   - vascular / stromal remodeling；
   - APC / antigen-presentation / IFN restoration；
   - T-cell activation / co-stimulation / exhaustion relief；
   - Treg suppression relief；
   - epigenetic immune priming；
   - tumor-intrinsic exclusion reversal；
   - TLS/B support（若证据足够）。
2. 每个 X-class 建立 structured descriptor：targets、pathways、expected repair direction、carrier states、known combination context、coverage sources。
3. 生成 barrier × X-class × source coverage matrix；
4. 对 myeloid/stromal/TLS/neutrophil 低覆盖显式标注，不得解读为反证据；
5. 对 exact drug、target family、mechanism class 三层做 identifier normalization；
6. B3 挂钩：
   - anti-TIGIT 等正向用例可作为预指定 transfer case；
   - LEAP-002、COSMIC-312 等临床失败组合仅作为 context-specific held-out challenge/negative evidence，不得当作该 X-class 在所有癌种中的普遍负标签。
7. 建立 semantic leakage 与 synonym leakage audit；
8. 冻结 `eligible_xclass_set_v0` 与 abstain classes。

并行技术方案：

#### 方案 A：Curated structured ontology + rule/evidence mapping（主线）

* 人工定义有限 X-class；
* targets/pathways/perturbation direction 规则归一；
* coverage 和冲突完全显式；
* 优点：可解释、易审计，适合后续 evidence card。

#### 方案 B：Attribute/text embedding + ontology alignment（增强路线）

* 将 barrier 与 X-class 的 gene/pathway/target/text descriptors 嵌入共同空间；
* 用 metric learning 或双编码器辅助相似性；
* 优点：支持未见 descriptor 与 Mechanism ZSL；
* 风险：文本语义和文献流行度可能产生虚假相似性，必须以 A 为锚。

#### 可选方案 C：Knowledge-graph normalization

* 构建 target-pathway-cell state-X-class evidence graph；
* 用于 identifier resolution 和证据追溯，不默认使用黑箱 GNN 排名。

评价指标：

* ontology coverage；
* inter-curator agreement；
* target/pathway mapping precision；
* direction consistency；
* source independence；
* coverage bias by barrier family；
* descriptor leakage；
* A/B nearest-class agreement 与 abstention behavior。

交付：

* `xclass_ontology_v6_2_1.yaml`
* `xclass_descriptor_table.csv`
* `barrier_xclass_perturbation_coverage_matrix.csv`
* `perturbation_direction_support.csv`
* `coverage_absent_vs_negative_evidence_registry.csv`
* `observed_combination_challenge_set.csv`
* `xclass_semantic_leakage_audit.md`
* `eligible_xclass_set_v0.csv`
* `phase10_to_phase11_handoff.yaml`

Gate：

* 未进入 eligible set 的 X-class 不得进入 Phase11B；
* 无 perturbation coverage 时允许做 attribute-only hypothesis，但必须降权并显式 abstain；
* 单一 LINCS reversal 不得成为 nomination；
* 方向冲突必须进入 conflict log，不能平均后消失；
* ontology class 数必须与有效监督/验证规模匹配，避免分类学膨胀。

Agent：

* perturbation/X-class agent 主执行；
* domain expert curator 与 leakage reviewer 并行；
* Controller 冻结 action space。

---

### Phase 11：Counterfactual Repair and X-conditioned Transport

目的：

在已冻结的 anchor direction、eligible barriers、representation route 和 X-class action space 上，分两步回答：

1. 反事实修复某 barrier 是否将状态推向 responder-compatible direction；
2. 哪些 eligible X-class 可能实现该修复方向。

本阶段输出是计算性机制假设，不是真实治疗效果。

#### Phase 11A：Generic Barrier Repair

任务：

1. 对每个 eligible barrier 定义 `do(repair)` 或受控 perturbation operator；
2. 估计 patient-level repair displacement；
3. 与 paired pre/post displacement、held-out state transition、negative controls 比较；
4. 计算 generic MRI / repair gain 与不确定性；
5. 排除未通过 identifiability gate 的 barrier。

#### Phase 11B：X-conditioned Repair

任务：

1. 将 eligible X-class descriptor 作为 condition；
2. 使用 coverage gate 调节 perturbation-consistency 权重；
3. 估计 X-conditioned repair displacement；
4. 对 observed combination / held-out challenge 做 L2 验证；
5. 输出 uncertainty、abstention 和 conflict flags。

并行技术方案：

#### 方案 A：线性 / local transport + Entropic OT（保守主线）

* nearest-responder / local linear displacement；
* entropic or unbalanced OT barycentric projection；
* module-level operator，参数少；
* 优点：适合有限患者、可与真实 paired displacement直接比较。

#### 方案 B：Conditional residual MLP / module-factorized transport（增强路线）

* 输入 patient module state + barrier + X descriptor；
* 输出局部 residual displacement；
* 施加 graph locality、coverage-gated perturbation consistency、calibration；
* 优点：可学习非线性和 context modulation；
* 风险：监督稀疏，必须明显超过方案 A 才能晋级。

#### 可选方案 C：Conditional flow / graph-constrained structural transport

* 仅在 paired/combination 数据和计算预算足够时作为 spike；
* 不能因“方法高级”自动进入主文。

评价指标：

* held-out displacement prediction；
* paired pre/post direction agreement；
* responder-direction movement；
* negative-control repair near zero；
* permutation null；
* graph/locality preservation；
* perturbation direction consistency；
* uncertainty calibration；
* A/B 路线稳定性与增量。

交付：

* `generic_barrier_repair_effects.csv`
* `patient_level_MRI_scores.csv`
* `repair_displacement_matrix.csv`
* `xclass_conditioned_repair_effects.csv`
* `transport_baseline_comparison.csv`
* `transport_calibration_and_null_report.md`
* `crtn_credibility_level.csv`
* `phase11_to_phase12_handoff.yaml`

Gate：

* Phase11A 不达标：关闭 model-based repair，保留 descriptive responder direction；
* Phase11B 只有在 coverage、held-out challenge 与 uncertainty 过门时才能形成 X-class repair hypothesis；
* 方案 B/C 不优于 A 时，采用 A；
* 无真实 combination / wet-lab 时，不写 proven causal therapeutic effect；
* repair 与后续 spatial/external 关键冲突时自动降级。

Agent：

* transport agent 主执行；
* OT baseline branch 与 neural transport branch 并行；
* perturbation agent提供 coverage；
* Controller 锁定 credibility level。

禁止：

* 将 counterfactual model 输出称为疗效；
* 把无 coverage 当作负证据；
* 未过 barrier gate 的 module 进入 repair；
* 无 baseline / null / calibration 的 transport 进入 evidence card。

---

### Phase 12A：Spatial Localization and Niche Adjudication

目的：

将空间数据作为组织生态裁判，判断 candidate barrier 的 carrier cells、局部邻近、T-cell exclusion、myeloid-tumor/APC-T/CAF-vascular niches 是否存在。默认先做可解释统计空间分析，只有数据支持时才训练 SNGM。

输入：

* standardized spatial assets from Phase2.5
* Phase11 candidate barriers / repair directions
* Phase7A carrier-state annotation
* LR database 与 module gene programs
* B2 TLS/APC-T niche 挂钩

任务：

1. spatial module projection / deconvolution / segmentation；
2. carrier-state localization；
3. tumor boundary / stromal boundary 定义；
4. T-cell exclusion / infiltration；
5. myeloid-tumor neighborhood；
6. APC-T / TLS niche；
7. CAF/ECM/vascular barrier；
8. LR spatial proximity；
9. coordinate / label / sample permutation null；
10. 输出 spatial support / conflict / not-testable，而不是强制正负二分。

并行技术方案：

#### 方案 A：Statistical niche baseline（默认主线）

* neighborhood enrichment、Ripley/KNN、boundary distance、permutation FDR；
* platform-specific 统计，不强行跨平台统一 graph；
* 优点：透明、适合当前 localization/support 级数据。

#### 方案 B：SNGM / interpretable graph model（条件增强）

* node/spot/cell graph + module/niche prediction；
* 只在 expression+coordinates+足够样本/标签时启用；
* 必须与 A 比较，并输出 attention/edge interpretation 的限制。

#### 可选方案 C：reference mapping / niche transfer

* 对无 response label 的空间数据做 module localization 和 cross-dataset niche transfer；
* 只能支持组织可行性，不支持 response association。

评价指标：

* spatial permutation FDR；
* patient/section reproducibility；
* cross-platform consistency；
* niche effect size；
* carrier-state localization；
* repair-direction spatial coherence；
* SNGM 相对 statistical baseline 的增量。

交付：

* `spatial_module_projection_scores.csv`
* `spatial_niche_statistics.csv`
* `spatial_permutation_null_results.csv`
* `candidate_spatial_support_conflict_matrix.csv`
* `sngm_model_manifest.yaml`（若启用）
* `spatial_claim_boundary.md`
* `phase12a_handoff.yaml`

Gate：

* 无 response/treatment label 的数据只能支持 localization / tissue feasibility；
* statistical niche 未通过时不得用 SNGM 漂亮图掩盖；
* strong TME mechanism claim 至少需要可重复 spatial support 或明确组织学验证计划；
* 空间不支持不等同于机制为假，需区分 coverage / resolution / biology conflict。

---

### Phase 12B：External Bulk and Clinical Direction Validation

目的：

在独立 bulk/clinical cohorts 中检验 barrier direction、context modulation 和 repair-compatible programs 的方向一致性，报告成功与失败案例。该 lane 与 Phase12A 并行。

输入：

* Phase8 barrier directions
* Phase11 repair programs
* external validation registry
* endpoint / treatment-arm metadata
* bulk-compatible module projection definitions

任务：

1. 冻结 bulk-compatible projection genes 与 scoring method；
2. 对每个 external cohort 单独投影，禁止先合并再评分；
3. 区分 PD1 mono、PD1+X、其他 ICI 和 non-ICI；
4. 执行 endpoint-specific association / survival sensitivity；
5. random-effects external meta-analysis；
6. 报告 failure cases、方向冲突和不可投影 modules；
7. 检验 HCC context residual 与 anti-VEGF/TKI-like repair programs；
8. 生成 candidate external support / conflict / not-testable 状态。

并行技术方案：

#### 方案 A：固定 module score projection + cohort-wise meta-analysis（主线）

* 使用预先冻结的 gene weights / ssGSEA / rank-based score；
* cohort 内标准化，cohort 间只合并 effect size；
* 优点：透明、避免 domain adaptation 隐藏偏差。

#### 方案 B：Reference-to-bulk latent projection / domain adaptation（增强路线）

* 从 scRNA module/pseudobulk 映射到 bulk latent；
* 可使用 regression、Bayesian deconvolution 或 domain-aligned encoder；
* 必须证明相对 A 提高方向一致而非仅拟合队列。

#### 可选方案 C：cell-type deconvolution-supported projection

* 将 bulk module direction拆成 composition 与 within-cell-state proxy；
* 作为机制边界敏感性分析。

评价指标：

* independent-cohort effect direction；
* random-effects heterogeneity；
* leave-one-cohort stability；
* treatment-arm specificity；
* endpoint sensitivity；
* projection coverage；
* failure-case rate；
* A/B external agreement。

交付：

* `external_module_projection_scores.csv`
* `external_validation_effects_by_cohort.csv`
* `external_validation_meta_analysis.csv`
* `external_repair_program_support.csv`
* `external_failure_and_conflict_cases.md`
* `candidate_external_support_conflict_matrix.csv`
* `phase12b_handoff.yaml`

Gate：

* 外部数据只验证方向，不反推 cell-cell/spatial mechanism；
* 不能投影的 module 必须标为 not-testable；
* 单一 cohort 成功不升级 L2/L3；
* 主候选至少需要一个独立 external direction support，或明确记录 external gap。

Agent 编排：

* Spatial agent 与 External validation agent 独立并行；
* 两者共享 frozen candidate IDs，但不得互相改写结果；
* Controller 在 Phase13 前执行 cross-lane conflict review。

Integration rehearsal 12：

Phase12A/B 结束后检查：

* spatial、external、transport 使用同一 barrier/X-class IDs；
* support / conflict / not-testable 三态区分；
* 是否有证据源依赖或重复计数；
* 哪些 candidate 可以进入 evidence card v0。

---

### Phase 13：Candidate Evidence Card v0 and Stage 2 Gate

目的：

在不依赖 ZSL 的前提下，将 barrier identifiability、anchor/context、repair、perturbation coverage、spatial 与 external validation 汇总为第一版可审计 evidence cards，并据此决定是否进入 Stage3 或启动可选 ZSL。

任务：

1. 为每个 candidate 定义唯一 ID：barrier、context、X-class、target（如有）；
2. 汇总五类核心证据：
   - identifiability / measurement；
   - anchor/context direction；
   - generic/X-conditioned repair；
   - perturbation coverage/direction；
   - spatial / external support；
3. 显式记录 conflict、not-testable 和 evidence dependency；
4. 生成 L1/L2/L3 provisional tier；
5. 检验是否至少一个 candidate card 无关键冲突；
6. 判断 ZSL 的监督/descriptor/held-out 条件是否成立；
7. 形成 Stage2 gate 与 Phase14 launch decision。

并行技术方案：

#### 方案 A：预注册规则证据矩阵（主线）

* 每类证据按强/中/弱/冲突/不可测试赋值；
* 关键冲突触发自动降级；
* 优点：透明，便于 hostile reviewer audit。

#### 方案 B：Bayesian evidence fusion（增强路线）

* 使用 permutation/null 校准每类 evidence likelihood；
* 输出 posterior claim probability 与可信区间；
* 必须审计证据条件独立假设和重复数据来源。

评价指标：

* tier stability under leave-one-evidence-source；
* conflict sensitivity；
* calibration against prespecified positive/negative cases；
* candidate ranking stability；
* rule/Bayesian agreement；
* evidence dependency audit。

交付：

* `candidate_evidence_card_v0.csv`
* `candidate_evidence_cards_v0.md`
* `candidate_conflict_log_v0.md`
* `evidence_dependency_matrix.csv`
* `provisional_claim_tier_v0.csv`
* `zsl_readiness_gate.yaml`
* `stage2_gate_v6_2_1.yaml`

Stage2 Go：

必须满足：

* 至少一个 L1 robust barrier/context result；
* barrier identifiability 至少部分通过；
* anchor route 明确；
* generic repair 或可信降级 repair direction 可解释；
* 至少一个 candidate evidence card v0 无关键方向冲突；
* spatial/external gap 已显式记录，而不是假装不存在。

否则：

* 不进入 ZSL 或 manuscript package；
* 返回 Phase7/8/10/11/12 中的具体 blocker；
* 允许锁定 Result Layer A resource/mechanism-atlas 退路。

ZSL launch 条件：

* 至少一个稳定 barrier；
* X-class ontology 有足够 seen classes 或 observed combinations；
* 能定义 leak-resistant held-out task；
* simple prototype/additive baseline 可运行；
* candidate card 无关键证据冲突。

Agent：

* Evidence integration agent 执行；
* Controller 独立裁定 Stage2 Go 与 ZSL launch；
* branch agents 不得自行升级 claim tier。

---

### Phase 14：Conditional Mechanism / Combination ZSL

目的：

仅在 Phase13 `zsl_readiness_gate` 通过时，检验结构化 mechanism space 是否支持未见 X-class、未见组合或未见机制描述的外推。ZSL 是增量证据层，不是机制证明和项目生死线。

任务：

1. 定义 Mechanism ZSL 与 Combination ZSL 两类任务；
2. 设计 leave-one-X-class / leave-one-combination / component-held-out split；
3. 检查 exact pair、synonym、target-family、文本描述泄漏；
4. 建立 nearest-prototype、attribute matching、additive compositional baseline；
5. 训练增强模型；
6. 输出 ranking、calibration、abstention；
7. 可选 Cancer ZSL 仅作为 LOCO validation，不进入主 claim；
8. 将 ZSL 结果回写 evidence card，但不得覆盖已有冲突。

并行技术方案：

#### 方案 A：Prototype / attribute / additive compositional baseline（强制主线）

* mechanism prototype similarity；
* PD1 anchor + X component additive repair；
* nearest-neighbor / matching-mean；
* 优点：低容量、可解释，是复杂模型必须超过的基准。

#### 方案 B：Dual-encoder / compositional ranking model（增强路线）

* patient/barrier encoder 与 X descriptor encoder；
* pairwise ranking + calibration + abstention loss；
* 可使用 module/repair attributes，不直接吃 raw expression；
* 优点：支持 semantic/compositional extrapolation；
* 风险：标签少、descriptor leakage。

#### 可选方案 C：Graph/niche-regularized multi-view ZSL

* 加入 causal-supporting graph、spatial niche、external attributes；
* 只在样本/类数量足够且方案 B 稳定后探索。

评价指标：

* Top-k accuracy / hit rate；
* NDCG / pairwise rank consistency；
* leave-one-class/combination performance；
* calibration error；
* abstention risk-coverage curve；
* semantic leakage null；
* relative gain over prototype/additive baseline；
* evidence-card consistency。

交付：

* `zsl_task_and_split_registry.csv`
* `zsl_leakage_audit.md`
* `zsl_baseline_results.csv`
* `mechanism_zsl_results.csv`
* `combination_zsl_results.csv`
* `zsl_calibration_and_abstention.csv`
* `zsl_adopt_reject_verdict.yaml`
* `phase14_to_phase15_handoff.yaml`

Gate：

* 复杂 ZSL 未稳定超过 simple baseline：降级为 descriptor/prototype support；
* 无有效 held-out 标签：关闭 Combination ZSL，只保留 Mechanism descriptor matching；
* calibration/abstention 不合格：不得输出强制排名；
* Cancer ZSL 失败不影响主线；
* ZSL 不得把 L1 candidate 自动升级为 L2/L3。

如果 Phase13 不允许启动 ZSL：

* Phase14 标记为 `SKIPPED_BY_GATE`；
* 直接进入 Phase15；
* 这不是项目失败。

---

## 6. Stage 3：Manuscript Package（v6.2.1 重排）

### Phase 15：Final Evidence Card and Result Layer Lock

目的：

将 Phase13 evidence card v0、可选 Phase14 ZSL、湿实验可行性与 claim boundary 合并，锁定最终 Result Layer 和主故事。此后不再无限增加分析。

任务：

1. 更新 candidate cards，保留 v0 与 final 的差异记录；
2. 重新计算 conflict / not-testable / evidence dependency；
3. 赋予 final L1/L2/L3 tier；
4. 锁定 Result Layer A/B/C；
5. 选择 main candidate、secondary candidate、negative/failure cases；
6. B3/B6 挂钩最终审阅；
7. 形成 wet-lab / tissue validation priority；
8. 冻结 manuscript claim boundary。

并行技术方案：

#### 方案 A：Rule-based final tier（默认）

* 沿用 Phase13 规则，加入 ZSL/wet-lab feasibility；
* 强制关键冲突降级。

#### 方案 B：Bayesian final evidence posterior

* 作为敏感性和排序辅助；
* 不替代规则型 hard boundary。

交付：

* `candidate_evidence_card_final.csv`
* `candidate_evidence_cards_final.md`
* `final_claim_tier.csv`
* `result_layer_lock.yaml`
* `main_story_decision.md`
* `tissue_and_wetlab_validation_priority.md`
* `claim_boundary_final.md`

Gate：

* 至少一个 Result Layer 完整成立；
* 主故事与证据 tier 匹配；
* 不允许“为了完整故事”隐藏失败候选；
* Result Layer C 只有在 ZSL、repair、spatial/external 同时满足时成立。

---

### Phase 16：Result Layer Assembly

目的：

将最终证据组织为一个可防守的论文结果结构，而不是继续做新模型。

Result Layer A：anti-PD1 state/barrier basis and context modulation

需要：

* response-blind module basis；
* measurement/identifiability；
* shared anti-PD1 barrier direction；
* context-modulated / HCC residual；
* external或空间至少一类支持；
* 不要求 model-based repair 成功。

Result Layer B：dominant barrier and repair hypothesis

需要：

* Layer A 成立；
* generic repair direction 通过；
* 至少一个 X-class 有 perturbation + spatial/external 的方向一致支持；
* claim 明确为 mechanism repair hypothesis。

Result Layer C：structured X-class extrapolation

需要：

* Layer B 成立；
* leak-resistant ZSL 任务；
* 复杂或简单 ZSL 至少稳定超过非信息性 null；
* calibration / abstention 合格；
* 至少一个 held-out transfer case 与 evidence card 一致。

并行组装方案：

#### 方案 A：Biology-first manuscript

* 以 shared/HCC barrier、spatial niche 和 external validation 为主；
* repair/ZSL 作为后半段增强。

#### 方案 B：Method-first manuscript

* 以 measurement-identifiability-repair-evidence card 框架为主；
* 仅在方法增量和跨队列验证足够时采用。

交付：

* `result_layer_A_package/`
* `result_layer_B_package/`
* `result_layer_C_package/`
* `result_layer_comparison_and_selection.md`
* `manuscript_result_outline.md`
* `main_supplement_boundary.md`

Gate：

* 只选一个主叙事；
* 其他层作为 secondary/supplement/future work；
* 不允许把三个结果层并列堆叠成三篇论文塞进一篇。

---

### Phase 17：Figure Source and Manuscript Package

目的：

把锁定 Result Layer 转成可追踪的 figure source、统计表、方法文档和 manuscript package。

任务：

1. 生成主图/补图 source tables；
2. 图形优先展示 effect size、CI、跨队列方向、failure cases；
3. 每张图绑定 script、input version、candidate ID、claim；
4. 生成 methods / statistical analysis / data provenance；
5. 生成 minimal reproducibility package；
6. 形成 manuscript first draft；
7. 不在此阶段新增模型或重新调参。

交付：

* `main_figure_source_registry.csv`
* `supplementary_figure_source_registry.csv`
* `figure_generation_manifest.yaml`
* `statistical_source_tables/`
* `methods_and_statistics.md`
* `data_code_availability.md`
* `minimal_reproducibility_package/`
* `manuscript_first_draft.md`
* `phase17_package_index.tsv`

Gate：

* 所有图可由 frozen source 复现；
* 图中文字与 claim boundary 一致；
* 不用 UMAP 或漂亮网络图代替统计证据；
* 失败/冲突结果在主文或补充中有位置。

---

### Phase 18：Hostile Reviewer Audit and Final Freeze

目的：

以最敌对的统计、肿瘤免疫、空间、机器学习和转化审稿标准，寻找足以拒稿的 hard blockers，并在投稿前降级、修复或删除 claim。

审查维度：

1. 数据与重复性；
2. endpoint 与 treatment confounding；
3. composition / lineage surrogate；
4. barrier identifiability；
5. anchor reliability；
6. SRB necessity；
7. counterfactual overclaim；
8. perturbation coverage bias；
9. spatial resolution / pseudo-replication；
10. external validation；
11. ZSL leakage / calibration；
12. evidence dependency；
13. wet-lab feasibility；
14. reproducibility。

并行审查方案：

* Reviewer A：hostile statistics / causal inference；
* Reviewer B：tumor immunology / HCC biology；
* Reviewer C：spatial / single-cell methods；
* Reviewer D：ML/ZSL / calibration；
* Controller：合并冲突并裁定 freeze。

交付：

* `hostile_reviewer_report_statistics.md`
* `hostile_reviewer_report_biology.md`
* `hostile_reviewer_report_spatial.md`
* `hostile_reviewer_report_ml.md`
* `claim_repair_or_deletion_log.csv`
* `final_reproducibility_audit.md`
* `FINAL_FREEZE_DECISION.yaml`
* `FINAL_PROJECT_REPORT.md`

Final freeze Gate：

* 无 hard blocker；
* 所有 claim 与 evidence tier 匹配；
* 所有 model-based counterfactual 有限制表述；
* figure source 可追踪；
* minimal reproducibility package 可运行；
* 不成立的 Result Layer 已删除或明确降级。

---

## 7. v6.2.1 AutoResearch-style 迭代总表

| 节点 | 必须保留的基线方案 | 并行增强方案 | 主指标 | 关闭/降级标准 |
|---|---|---|---|---|
| Phase7A measurement | mixed-effect residualization | Bayesian hierarchical measurement | residual reproducibility, variance decomposition | 无法拆分 composition → context/sensitivity only |
| Phase7B identifiability | deterministic bootstrap/collinearity gate | probabilistic factor separation | flip rate, condition number, merge stability | 不可分 → coarse barrier |
| Phase8 anchor/context | within-cohort + random-effects meta | hierarchical Bayesian partial pooling | direction consistency, heterogeneity, LOCO | anchor 不稳 → R1-lite/R2/Layer-A-only |
| Phase9 representation | direct module backbone | SRB Stage-A / hierarchical latent | leakage, interpretability, downstream utility | SRB 无增量 → 关闭 SRB |
| Phase10 ontology | curated structured ontology | dual-encoder / KG alignment | coverage, mapping precision, leakage | 类过多/覆盖低 → 合并或 abstain |
| Phase11 repair | linear/local + OT | residual MLP / conditional flow | held-out displacement, null, calibration | 不超基线 → descriptive/OT repair |
| Phase12A spatial | statistical niche + permutation | SNGM | FDR, reproducibility, localization | 数据不足 → localization only |
| Phase12B external | fixed projection + meta-analysis | domain-aligned projection | independent direction, heterogeneity | 无复现 → conflict/gap |
| Phase13/15 evidence | rule matrix | Bayesian fusion | tier stability, conflict sensitivity | evidence dependent/冲突 → 降级 |
| Phase14 ZSL | prototype/additive | dual-encoder / graph-niche regularized | held-out ranking, calibration, abstention | 不超基线/无标签 → skip or descriptor-only |

统一迭代规则：

1. 每个探索 phase 至少同时保留一个低容量可解释路线和一个增强路线；
2. 每轮只改变一个主要因素；
3. 所有路线写入统一 eval registry；
4. 方案 B 不因模型复杂度自动获胜；
5. 只采纳在预注册指标上有稳定增量且不增加关键冲突的方案；
6. 失败实验进入 iteration log，不删除；
7. route verdict 由 Controller 冻结。

---

## 8. Agent / Session 切换规则（v6.2.1）

### 8.1 推荐并行结构

Phase7：

* measurement route A agent；
* measurement route B agent；
* hostile measurement reviewer。

Phase8：

* meta-analysis agent；
* hierarchical model agent；
* endpoint adjudication reviewer。

Phase9：

* direct backbone agent；
* SRB agent；
* representation leakage reviewer。

Phase10–11：

* ontology/coverage agent；
* OT repair agent；
* neural transport agent。

Phase12：

* spatial statistical agent；
* optional SNGM agent；
* external validation agent。

Phase13–15：

* evidence integration agent；
* optional ZSL baseline agent；
* optional ZSL enhanced agent；
* Controller。

### 8.2 Branch agent 最小输出

每个 branch 必须输出：

* `handoff_manifest.yaml`
* `decision_log.md`
* `eval_record.yaml`
* `evidence_items.csv`
* `conflict_or_failure_log.md`
* `adopt_reject_recommendation.md`

### 8.3 禁止 branch 自行决策的事项

* 重新开放 Phase6 module discovery；
* 升级 candidate claim tier；
* 合并 heterogeneous endpoints；
* 启用 SRB / SNGM / ZSL 主线；
* 选择最终 Result Layer；
* 删除失败/冲突证据。

这些由 Controller 决定，必要时提交用户确认。

### 8.4 v6.2.1 Integration rehearsal 节点

1. Phase7A/B 后：measurement + identifiability；
2. Phase8 后：anchor/context route；
3. Phase9 后：representation route；
4. Phase11 后：repair credibility；
5. Phase12A/B 后：orthogonal evidence；
6. Phase13 后：Stage2 gate；
7. Phase14 后（若启用）：ZSL adoption；
8. Phase15 后：Result Layer lock；
9. Phase17 后：manuscript package；
10. Phase18 final freeze。

---

## 9. Go / No-Go 总规则（v6.2.1）

### Phase7 Go

需要：

* Phase6 frozen module entry 唯一；
* patient-timepoint scoring contract 完成；
* composition/lineage/QC source 已审计；
* 至少一个 eligible barrier 或 coarse barrier family。

否则：

* 不进入 supervised anchor；
* 项目降级为 response-blind state/resource story。

### Anchor/Context Go

需要：

* expression-bearing anchor environment 冻结；
* endpoint 分层；
* 至少一个 barrier 跨队列方向可解释；
* route verdict 明确。

否则：

* 关闭 R1；
* 进入 R2 或 Result Layer A-only。

### Representation Go

需要：

* direct backbone 与 SRB 公平比较；
* route 无关键 leakage；
* 表示保留 module 解释性。

否则：

* 使用 direct module backbone。

### Repair Go

需要：

* barrier 过 identifiability；
* anchor direction 冻结；
* X-class ontology/coverage 已冻结；
* baseline/null/calibration 可运行。

否则：

* 只做 descriptive repair direction。

### Stage2 Go

需要：

* 至少一个 L1 robust result；
* 至少一个 candidate card v0 无关键冲突；
* spatial/external 证据或明确 gap；
* repair credibility 已分级；
* ZSL readiness 已裁定。

否则：

* 不进入 Stage3 full manuscript package；
* 锁定较低 Result Layer 或回补具体 blocker。

### Stage3 Go

需要：

* Result Layer lock；
* final evidence card；
* claim boundary；
* hostile review 无 hard blocker；
* reproducibility package 可运行。

---

## 10. v6.2.1 最终施工顺序压缩版

已完成并保持原文：

Phase 0.1 Project Control Brief
Phase 0.2 Evaluation Harness
Phase 1 Source-of-truth and Inheritance Lock
Phase 2 Data Go / No-Go Audit
Phase 2.5 Spatial Data Standardization Gate
Phase 3 Frozen Input Family
Phase 3.5 Single-cell Processing QC Gate
Phase 4A Single-cell Integration and Cell-state Harmonization
Phase 4B Patient-timepoint Immune-state Feature Construction
Phase 5 Strong Baseline and Confounding Audit
Phase 6 Response-blind Module Discovery

v6.2.1 新下游：

Phase 7A Frozen Module Semantic and Measurement Validity
Phase 7B Barrier Identifiability Gate
Phase 8 Anti-PD1 Anchor and Context Adjudication
Phase 9 SRB Necessity and Representation Gate
Phase 10 Perturbation Coverage and X-class Descriptor Ontology
Phase 11 Counterfactual Repair and X-conditioned Transport
Phase 12A Spatial Localization and Niche Adjudication
Phase 12B External Bulk and Clinical Direction Validation
Phase 13 Candidate Evidence Card v0 and Stage 2 Gate
Phase 14 Conditional Mechanism / Combination ZSL
Phase 15 Final Evidence Card and Result Layer Lock
Phase 16 Result Layer Assembly
Phase 17 Figure Source and Manuscript Package
Phase 18 Hostile Reviewer Audit and Final Freeze

核心路线：

**冻结 Phase6 topics → 拆解测量来源 → 过 barrier 可辨识性门 → 先建立 anchor/context direction → 决定是否需要 SRB → 定义 X-class 与 coverage → generic/X-conditioned repair → spatial 与 external 并行裁判 → evidence card v0 → 条件性 ZSL → 锁 Result Layer → manuscript package。**

---

## 11. v6.2.1 的关键判断

1. Phase6 已完成，真正需要的是封口和降自由度，而不是重新选算法。
2. 8 个 frozen topics 是候选测量坐标；经过 Phase7 后减少为 3–6 个 coarse/eligible barriers 完全可以接受。
3. response association 必须建立在 measurement validity 与 identifiability 之后，不能让 Phase7 同时选择和证明 response module。
4. anti-PD1 anchor/context 必须先于复杂 SRB；否则监督噪声会被包装进 latent。
5. SRB、SNGM、ZSL 都是条件模型，不是必须全部启用的装饰套件。
6. X-class ontology 和 perturbation coverage 必须先于 X-conditioned transport。
7. spatial 与 external validation 是 evidence card v0 的输入，不是卡片完成后的补图。
8. ZSL 只有在稳定 barrier、可定义 action space 和 leak-resistant held-out task 同时存在时才有科学意义。
9. Result Layer A 是独立可成立的主线，不应因为 Layer B/C 失败被视为项目失败。
10. 最危险的不是模型不够复杂，而是把 composition、组织身份和 endpoint 混杂做成一个看起来很高级的机制模型。

v6.2.1 保留 v6.2 的科学野心，但将方法学自由度放在正确的依赖顺序上，并为每个复杂模型设置低容量、可解释、可失败的并行基线。
