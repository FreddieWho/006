# Archived scripts

这里的脚本仅用于历史重放、故障定位或审计，不是 v7 主入口。

- `v6_2_diagnostics/`：曾位于根 `scripts/` 的临时检查、调试和参数试验脚本。
- `v6_data_acquisition/`：旧数据下载入口，只用于来源追溯与必要时重放。
- `v5_scaffold/`：旧目录脚手架，不适用于 v7 结构。

历史版本化主流程仍保留在 `scripts/v6_1/` 与 `scripts/v6_2/`，避免破坏结果 provenance。
